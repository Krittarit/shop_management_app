name: shop_management_app
description: แอปพลิเคชันจัดการร้านค้าอย่างง่าย

# Remove this line if you wish to publish to pub.dev
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.5
  firebase_core: ^2.32.0
  firebase_auth: ^4.9.0
  cloud_firestore: 4.17.5
  provider: ^6.0.5
  flutter_slidable: ^3.0.0
  image_picker: ^1.0.4
  firebase_storage: ^11.7.7
  cached_network_image: ^3.2.3
  fl_chart: ^0.64.0
  intl: ^0.19.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true